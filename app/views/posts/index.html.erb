<% flash.each do |name, msg| %>
  <%= msg %>
<% end %>

<h1>Posts Index</h1>

<%= link_to 'Add a Post', new_post_path %>

<% @posts.each do |post| %>
  <h3><%= post.title %></h3>
  <p>
    <%= post.content %>
  </p>
<% end %>
